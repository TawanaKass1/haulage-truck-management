
services:
  backend:
    build: ./backend
    container_name: haulage-backend
    environment:
      NODE_ENV: production
      PORT: 5000
      DATABASE_URL: postgresql://neondb_owner:npg_QwJ3GKSu2AZE@ep-broad-night-anlzhz1j.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
      JWT_SECRET: TAWA1
      JWT_EXPIRE: 7d
      EMAIL_USER: tawananyashakaseke@gmail.com
      EMAIL_PASS: Kaseke123@
    ports:
      - "5000:5000"
    networks:
      - haulage-network
    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    build: ./frontend
    container_name: haulage-frontend
    environment:
      VITE_API_URL: http://localhost:5000/api
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - haulage-network
    volumes:
      - ./frontend:/app
      - /app/node_modules

networks:
  haulage-network:
    driver: bridge